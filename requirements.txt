# For Telegram Bot
python-telegram-bot[job-queue]~=21.6

# For timezone handling
pytz~=2025.1

# For CSV exports (standard library handles csv, so not needed)
# For sqlite3 (built-in, no extra install needed)
